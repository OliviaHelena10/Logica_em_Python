# for in com tipo list
lista = ['Olívia', 'Edeildo', 'Maria', 'Elvis']
indices = range (len(lista))

for name in lista:

    print (indices, name)
    
for indice in indices:
    
    print (indice, lista[indice])